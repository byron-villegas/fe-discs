import { ReplaceAllPipe } from './replace-all.pipe';

describe('Pipe: ReplaceAlle', () => {
  it('create an instance', () => {
    let pipe = new ReplaceAllPipe();
    expect(pipe).toBeTruthy();
  });
});
