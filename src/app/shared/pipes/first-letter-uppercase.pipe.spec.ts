import { FirstLetterUppercasePipe } from './first-letter-uppercase.pipe';

describe('Pipe: FirstLetterUppercasee', () => {
  it('create an instance', () => {
    let pipe = new FirstLetterUppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
